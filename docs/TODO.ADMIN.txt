Make it possible to build stats from all calls to the API.
    Put the information in a static servlet.
    Push the data to the db every now and then.

Show FeedAppConfig values in admin.

Send emails from b_gaveup sources.
    View given up sources. 
    Make it possible to add a new source entry and link the old one to that.
        That way it is possible for people to follow on changes.
    ... related ...
    We can use the news title to check for duplicated sources. Same source, with different URL.

Log needs to go out to a file, not system.out.

Refresh by stream on admin, run_fetch.
