http://localhost:8080/feedreader/pages/reader.jsp#/s/13867
	The source had 330 entries but none where showing, I had to debug with RealImporTest to find out that the 
	publication date was -1 and the fallback wasn't setting the XmlEntry.setPubData, so it reached the database with -1.
	We need ?DEBUG=1 param that shows data on the page.
	
Option to start fetch threads/stop without redeploying.

Save users OPML files to test in DEV. Archive.

Choose a build system: ANT vs Maven vs Gradle.
    http://technologyconversations.com/2014/06/18/build-tools/

For Dev when not fetching, we need an easy way to validate/run.

